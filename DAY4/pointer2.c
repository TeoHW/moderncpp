// 특정 변수의 포인터 변수를 만드는 방법
// 1. 변수의 주소를 구하려면 "&변수이름"
// 2. 포인터 변수를 만들려면 "변수와 동일하게 선언후, 이름앞에*" 붙이면됩니다.
int main()
{
	int n = 0;
	int *p1 = &n;

	double d = 0;
	? p2 = &d;

	int x[3] = {1,2,3};
}